package com.wei.uniform;

public class UniformService {

    public UniformResult doRequest(UniformRequest request) {
        // 基本参数过滤

        // 根据请求类型获取参数校验规则

        // 校验规则引擎内容，返回检验结果

        // 执行请求流程

        return new UniformResult();
    }
}
